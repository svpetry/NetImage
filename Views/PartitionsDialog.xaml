<Window x:Class="NetImage.Views.PartitionsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Partitions" Height="300" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F0F0F0">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" Margin="0,0,0,10"
                  ItemsSource="{Binding}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  Background="White"
                  AlternatingRowBackground="#FAFAFA">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Index" Binding="{Binding Index}" Width="Auto" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsBootable}" Width="Auto" />
                <DataGridTextColumn Header="Type" Binding="{Binding Type, StringFormat=0x\{0:X2\}}" Width="Auto" />
                <DataGridTextColumn Header="Name" Binding="{Binding TypeName}" Width="*" />
                <DataGridTextColumn Header="Start Sector" Binding="{Binding StartSector}" Width="Auto" />
                <DataGridTextColumn Header="Sectors" Binding="{Binding SectorCount}" Width="Auto" />
                <DataGridTextColumn Header="Size (MB)" Binding="{Binding SizeMB, StringFormat=\{0:F1\}}" Width="Auto" />
            </DataGrid.Columns>
            <DataGrid.ItemContainerStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Margin" Value="0,0,0,2"/>
                </Style>
            </DataGrid.ItemContainerStyle>
        </DataGrid>

        <Button Grid.Row="1" Content="Close" Width="75" Height="24" HorizontalAlignment="Right" IsCancel="True" Click="CloseButton_Click" />
    </Grid>
</Window>
