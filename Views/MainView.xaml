<Window x:Class="NetImage.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetImage.Views"
        xmlns:vm="clr-namespace:NetImage.ViewModels"
        Title="{Binding WindowTitle}" Height="450" Width="840" MinWidth="600" MinHeight="300"
        Icon="/diskette.ico">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Menu Bar -->
        <ToolBarTray Grid.Row="0" Background="Transparent">
            <ToolBar>
                <Button Command="{Binding NewCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE710;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="New" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Separator />
                <Button Command="{Binding OpenCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8E5;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Open" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding SaveCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE74E;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Save" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding SaveAsCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE792;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Save As" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding CloseCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8BB;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Close" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Separator />
                <Button Command="{Binding CreateFolderCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8F4;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="New Folder" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding AddFolderCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8B7;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Add Folder" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding AddCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE710;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Add" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DeleteCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE74D;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Delete" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ExtractCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xEDE1;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Extract" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Separator />
                <Button Command="{Binding EditCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE70F;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Edit" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding RenameCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE8AC;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Rename" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Separator />
                <Button Command="{Binding FormatCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE928;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Format" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding BootSectorCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE967;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Bootsector" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding ImageMapCommand}" Background="Transparent" BorderThickness="0" Margin="2,0">
                    <StackPanel Orientation="Vertical" Margin="4">
                        <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="&#xE9F9;" FontSize="20" HorizontalAlignment="Center"/>
                        <TextBlock Text="Image Map" HorizontalAlignment="Center" Margin="0,2,0,0" FontSize="11"/>
                    </StackPanel>
                </Button>
            </ToolBar>
        </ToolBarTray>

        <!-- Content Area -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="7*"/>
            </Grid.ColumnDefinitions>

            <!-- Left pane: Folders Tree -->
            <TreeView Grid.Column="0" Name="MainTreeView" ItemsSource="{Binding TreeItems}" BorderThickness="0" Padding="0,4">
                <TreeView.Resources>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="Black"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent"/>
                    <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="Black"/>
                </TreeView.Resources>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    </Style>
                </TreeView.ItemContainerStyle>
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <Border x:Name="SelectionBorder" BorderThickness="1" BorderBrush="Transparent" CornerRadius="3" Padding="4,2,6,2" Margin="2,0,2,0">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="{Binding IconGlyph}" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0" Foreground="#EDA72B"/>
                                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" FontSize="14"/>
                            </StackPanel>
                        </Border>
                        <HierarchicalDataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                <Setter TargetName="SelectionBorder" Property="Background" Value="#CCE8FF"/>
                                <Setter TargetName="SelectionBorder" Property="BorderBrush" Value="Black"/>
                            </DataTrigger>
                        </HierarchicalDataTemplate.Triggers>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
            </TreeView>

            <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="Transparent"/>

            <!-- Right pane: Files/Folders List -->
            <ListView Grid.Column="2" Name="MainListView" ItemsSource="{Binding CurrentFolder.Items}" BorderThickness="1,0,0,0" BorderBrush="#EEEEEE" SelectionChanged="MainListView_SelectionChanged">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="4,2"/>
                        <EventSetter Event="MouseDoubleClick" Handler="MainListViewItem_MouseDoubleClick"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.View>
                    <GridView AllowsColumnReorder="False">
                        <GridViewColumn Width="32">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets" Text="{Binding IconGlyph}" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsFolder}" Value="True">
                                                        <Setter Property="Foreground" Value="#EDA72B"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Name}" Width="250">
                            <GridViewColumn.Header>
                                <TextBlock Text="Name" Tag="Name"/>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding FormattedModified}" Width="150">
                            <GridViewColumn.Header>
                                <TextBlock Text="Date modified" Tag="Modified"/>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                        <GridViewColumn Width="100">
                            <GridViewColumn.Header>
                                <TextBlock Text="Size" Tag="Size"/>
                            </GridViewColumn.Header>
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding FormattedSize}" TextAlignment="Right" HorizontalAlignment="Right" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2">
            <StatusBarItem Padding="4,0">
                <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center"/>
            </StatusBarItem>
            <StatusBarItem Padding="4,0" Visibility="{Binding IsProgressVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                <StackPanel Orientation="Horizontal">
                    <ProgressBar Width="180"
                                 Height="14"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="{Binding ProgressValue, Mode=OneWay, FallbackValue=0, TargetNullValue=0}"
                                 IsIndeterminate="{Binding IsProgressIndeterminate}"
                                 Margin="0,0,8,0"/>
                    <TextBlock Text="{Binding ProgressText}" VerticalAlignment="Center"/>
                </StackPanel>
            </StatusBarItem>
            <StatusBarItem Padding="4,0" DockPanel.Dock="Right" HorizontalContentAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding FilesystemTypeText}" VerticalAlignment="Center" Foreground="Gray"/>
                    <TextBlock Text=" | " VerticalAlignment="Center" Foreground="Gray"/>
                    <TextBlock Text="{Binding DiskSpaceText}" VerticalAlignment="Center" TextAlignment="Right"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
